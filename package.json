{
	"name": "pi-subagents",
	"version": "0.1.0",
	"description": "Persistent, addressable subagents for pi: markdown-defined delegates with their own context window, @name references, and a live full-screen viewer.",
	"keywords": ["pi-package", "pi", "subagents", "agents"],
	"license": "MIT",
	"type": "module",
	"pi": {
		"extensions": ["./extensions"]
	},
	"peerDependencies": {
		"@earendil-works/pi-agent-core": "*",
		"@earendil-works/pi-ai": "*",
		"@earendil-works/pi-coding-agent": "*",
		"@earendil-works/pi-tui": "*",
		"typebox": "*"
	}
}
